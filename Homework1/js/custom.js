var height = prompt ('Введите высоту цилиндра в сантиметрах');
var radius = prompt ('Введите радиус цилиндра в сантиметрах');
var vol = Math.PI*(radius*radius)*height;
alert ('Объем цилиндра с высотой ' + height + 'см и радиусом ' + radius + 'см равен: ' + vol);